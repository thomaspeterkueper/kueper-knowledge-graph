KB-REQUEST

ID:
REQ:L3:PENDING

Requester:
SYS:KUEPER:ssf

Recipient:
SYS:KUEPER:knowledge-graph

Request Type:
schema_request

Purpose:
`OTA-ARC-0005-2026-DE` defines `Archivmodul` as one of three SSF module types. With multiple document sources, the type should be source-agnostic and not implicitly OTA-only. The existing relation pattern already supports this through namespaced PATH and DOC IDs.

Requested Content:

1. Add `archivmodul` to the `meta.type` vocabulary for `records.learning_modules` together with `grundmodul` and `brueckenmodul`.
2. Document that `PATH:<system>:<doc-id>:READ --TARGETS--> DOC:<system>:<doc-id>` is the canonical Archivmodul linking pattern for any registered document source.
3. Complete missing `REQUIRES` relations in `relation-registry-0.1.json` for existing OTA documents so Archivmodul prerequisite rendering is complete.

Awareness:
`OTA-ARC-0005-2026-DE` still uses OTA-specific wording. Revising that standard is a separate curatorial decision.

Priority:
medium

Blocking:
No current Archivmodul is blocked today, but this should be resolved before the first one ships.

Suggested IDs:
n/a

Target Export:
exports/relation-registry-0.1.json and learning module type documentation

Status:
open

Created:
2026-07-03

Curator:
T.P.K.