KB-REQUEST

ID:
REQ:L3:PENDING

Requester:
SYS:KUEPER:ssf

Recipient:
SYS:KUEPER:knowledge-graph

Request Type:
domain_request

Purpose:
`MAT-1101`'s `module_unlocks` includes `CHE-1101`, but no source document exists anywhere in OTA's 212 documents that fits a chemistry-foundations role - checked via keyword search (Periodensystem, Molekuelbindung, Reaktionskinetik and related terms). What chemistry content exists is advanced/niche (interstellar sulfur chemistry, hydrothermal neurotoxicology) - N2/N3 specialist material, not a `CHE-1101`-appropriate entry point.

This is not an OTA gap to fill - OTA is a research archive, not a textbook source, and forcing a "chemistry basics" document into it would work against its own character. `KG-0011` already established `kueper-com`'s role as covering exactly this kind of MINT-plus foundational content (`owns: draft_learning_content`, scope explicitly includes `CHE`).

Requested Content:

Confirm `kueper-com` as the intended source for `CHE-1101`'s underlying content, consistent with `KG-0011`. No OTA document search needed once this is confirmed - route the request to `kueper-com`'s own `external-tasks/open/` inbox instead of asking OTA for something outside its scope.

Priority:
low

Blocking:
`CHE-1101` itself already exists as a stub (`status: planned`) - this doesn't block the module's existence, only its eventual real content and, if ever wanted, an archive-document destination the way `MAT-1102`'s branch has one in `OTA-SCI-0083`.

Suggested IDs:
n/a

Target Export:
n/a - routing/confirmation question, not a data change.

Status:
open

Created:
2026-07-10

Curator:
T.P.K.
