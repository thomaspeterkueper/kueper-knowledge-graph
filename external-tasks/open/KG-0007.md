KB-REQUEST

ID:
REQ:L3:PENDING

Requester:
SYS:KUEPER:knowledge-graph

Request Type:
migration_request

Purpose:
`exports/learning.json` und `exports/kxf-learning-modules-0.1.json` beschreiben beide `LRN:SSF:*`-Lernmodule, aber mit unterschiedlicher Form und teils unterschiedlichem Inhalt. Ohne klare Kennzeichnung kann ein künftiger Konsument `learning.json` fälschlich als aktiven Vertrag behandeln.

Requested Content:

Markiere `exports/learning.json` explizit als Legacy-/Arbeits-Export.

Beispiel:

`"status": "internal_working_export"`

und

`"consumerContract": false`

Zusätzlich Verweis auf:

`exports/kxf-learning-modules-0.1.json`

als aktiven SSF-Vertrag.

Keine neuen Systeme sollen `learning.json` als konsumierbaren Export verwenden.

Zusätzlich Dokumentation in `docs/KB-REQUEST-0006-KXF-LEARNING-MODULES.md`, dass `learning.json` kein aktiver Consumer-Vertrag ist.

Priority:
low

Blocking:
Nichts aktuell. Reine Absicherung gegen zukünftige Verwechslung.

Suggested IDs:
n/a

Target Export:
exports/learning.json

Status:
open

Created:
2026-07-02

Curator:
T.P.K.