KB-REQUEST

ID:
REQ:L3:PENDING

Requester:
SYS:KUEPER:ssf

Recipient:
SYS:KUEPER:knowledge-graph

Request Type:
system_request

Purpose:
The taxonomy contains language, history and philosophy domains that are not yet fed by module content. KG-0006 also deferred methodology and reading-skill modules because `TEC` has no SSF subject home. A registered system should be designated as the normal originator for foundational general-knowledge draft content without changing KG's source-of-truth role.

Requested Content:

1. Extend `SYS:KUEPER:kueper-com` in `exports/system-registry-0.1.json` with `draft_learning_content` in `owns`.
2. Scope `draft_learning_content` to foundation-level MINT-plus units, including STEM, language, history and philosophy domains.
3. State that `kueper-com` drafts content and submits it as entity requests. The Knowledge Graph remains the canonical curator and source of truth.
4. Once accepted, content exports like any other learning module through `exports/kxf-learning-modules-0.1.json` and is consumed by SSF.

Downstream consequence:
SSF may later need subject taxonomy and brand-positioning updates for language, history and philosophy content. Not requested here.

Priority:
medium

Blocking:
Nothing currently blocked. LANG/HIS/PHIL and deferred TEC-style content remain unfed until resolved.

Suggested IDs:
n/a

Target Export:
exports/system-registry-0.1.json -> records.SYS:KUEPER:kueper-com

Status:
open

Created:
2026-07-03

Curator:
T.P.K.