# External Tasks

This folder collects incoming requests from sibling projects.

Folders:

- open: new requests
- done: completed requests
- rejected: declined requests

Use one markdown file per request.
